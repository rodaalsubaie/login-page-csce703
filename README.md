# Basic Login Form

This project contains a simple login form with email and password fields.

## Validation

The form checks that:

- Both fields are filled in
- The email contains `@`
- The password is at least 8 characters long

The same checks are repeated on the server.

## Run

```bash
npm install
npm start
```

Open:

```text
http://localhost:3000
```
